---
title: 'Seed post — pipeline check'
date: '2026-08-23'
description: 'Draft post used to verify the blog pipeline end to end. Never published.'
draft: true
---

This post exists to verify the blog pipeline: frontmatter parsing, MDX rendering,
code blocks, and static generation. It is marked `draft: true` and never appears
in the listing, the home page, the sitemap, or the RSS feed.

## A heading

Some **bold text**, a [link](https://alvarofreire.es), and inline `code`.

```python
def eval_task(sample):
    result = agent.run(sample.input)
    return score(result, sample.expected)
```

> A blockquote to check the signal-yellow rule.

- A list item
- Another list item
